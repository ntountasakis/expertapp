syntax = "proto3";

package call_transaction_package;

service CallTransaction
{
    rpc InitiateCall(CallRequest) returns (CallMessage) {}
}

message CallRequest {
    string callerUid = 1;
    string calledUid = 2;
}

message CallMessage {
    bool success = 1;
    string errorMessage = 2;
}